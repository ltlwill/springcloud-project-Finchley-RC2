# 加解密密匙配置(非对称加解密) ,使用jdk中自带的keytool工具,命令行如：keytool -genkeypair -alias ms-config-server -keyalg RSA -keystore ms-config-server.keystore
encrypt:
  key-store:
    location: classpath:/keystore/config-server.keystore
    alias: config-server
    secret: 111111
    password: 111111